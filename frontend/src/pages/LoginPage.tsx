import { Title, Text, Stack } from '@mantine/core';

const LoginPage = () => (
  <Stack py="lg">
    <Title order={2}>Login</Title>
    <Text c="dimmed">(Placeholder: il form arriverà dopo. Qui ora ci interessa solo il routing.)</Text>
  </Stack>
);

export default LoginPage;
